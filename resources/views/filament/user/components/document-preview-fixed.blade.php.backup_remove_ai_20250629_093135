<div class="document-preview">
    @if($fileType === 'pdf')
        <iframe src="{{ $fileUrl }}" class="w-full h-96 border rounded"></iframe>
    @else
        <img src="{{ $fileUrl }}" 
             alt="Anteprima documento" 
             class="w-full max-h-96 object-contain border rounded bg-gray-50"
             style="aspect-ratio: auto; max-width: 100%; height: auto;">
    @endif
</div>
